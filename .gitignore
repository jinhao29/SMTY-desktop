node_modules/
android_app/
dist/
build/
__pycache__/
*.pyc
.uploads/
.reasonix/
.codegraph/
*.log
# 备份文件（.bak / .orig 等）不入库，避免仓库冗余
*.bak
*.orig

# 运行时元数据
meta_index.db*
# 桌面端自动备份产物（App 运行时实时生成，不入库）
AutoBackups/
# 文件锁伴生文件（file_lock 保留锁文件不删除，见 file_lock.py）
*.lock

# 签名密钥与凭据（严禁提交）
SMTY
*.keystore
*.jks
*.p12
*.pfx
*.smty_backup
keystore.properties

# Android UI Automator 调试转储（ui*.xml）
ui*.xml

# 工具与临时资料（不入库）
.workbuddy/
# 代码审查报告为本地生成的时效性产物，全版本一并忽略（v1/v2/v3 规则统一）
code_review_report*.html
深圳体育中考.pdf

# 重构过程临时产物（验证截图 / 一次性验证脚本，用完即弃）
_refactor_preview*.png
_verify_*.py

# 个人/时效性业务文件不入库（2026-09-04 随恢复快照误入，移出跟踪）
秦宝腾.xlsx
每日财经要闻-*.html

# 仓库根运行时恢复备份（backup_restorer 自动生成，不入库）
backups/

# 一次性事故恢复脚本与 UI 预览调试脚本（用完即弃）
student_sports_tool/_recover_*.py
student_sports_tool/_render_*.py
_ui_fix_preview/
student_sports_tool/_ui_fix_preview/

# 同步/技术审查报告（本地生成的时效性产物）
sync_audit_*.html
tech_review_*.html
top_whitespace_audit_report.html

# 发版打包产物（make_release.py 输出，上传 GitHub Release 用，不入库）
student_sports_tool/release/

# === 2026-09-10 补齐（代码审查 P2-17 死代码/调试产物污染）===
# 小程序后端验证脚本与启动脚本（run_server.cmd 含明文 MP_SECRET，严禁入库）
_mp_verify/
# 更新链路真机测试产物（APK/ZIP/截图）
_update_test/
# UI 预览截图（渲染脚本输出，纯过程产物）
ui_preview/
student_sports_tool/ui_preview/
# 数据库时光机快照（meta_index_store 运行时生成，可随时重建）
_db_snapshots/
**/_db_snapshots/
# 代码审查报告（本地生成，时效性产物）
code_review_*.md
# 桌面端运行时同步备份
.sync_backups/
student_sports_tool/.sync_backups/
# 凭据修复前的临时备份（不可入库）
miniprogram/server/_backup_before_cred_fix/
# 小程序运行时数据与虚拟环境
miniprogram/server/server_data/
miniprogram/node_modules/
miniprogram/dist/
# 训练工具一次性验证脚本
training_tool/_verify_*.py
